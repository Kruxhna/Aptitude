---
phase: 7
plan: 2
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - client/app/(tabs)/index.tsx
  - client/app/(tabs)/sprint.tsx
  - client/app/sprint/[type].tsx
  - client/app/sprint/results.tsx
  - client/src/components/QuestionCard.tsx
  - client/src/components/TimerBar.tsx
  - client/src/components/SkillBadge.tsx
autonomous: true
requirements:
  - SPRT-01
  - SPRT-02
  - SPRT-03
  - SPRT-04
  - SPRT-05
  - SPRT-06
  - SPRT-07
  - SPRT-08
---

<objective>
Implement the full interactive sprint flow: length selection (quick/standard/deep), question card rendering (MCQ, numerical, spatial), countdown timer bar (D-49), auto-advance answer behavior (D-50), sprint submission, and results summary modal with answer review and rating deltas.
</objective>

<tasks>

<task>
<id>07-02-01</id>
<type>code</type>
<files>client/src/components/SkillBadge.tsx, client/src/components/TimerBar.tsx, client/src/components/QuestionCard.tsx</files>
<action>
1. Create client/src/components/SkillBadge.tsx: Renders skill name with LinearGradient background based on skill gradient colors (D-52).
2. Create client/src/components/TimerBar.tsx: Horizontal animated progress bar (D-49) representing remaining time for the question. Changes color to red (#EF4444) when remaining time drops below 25%. Triggers timeout callback on completion.
3. Create client/src/components/QuestionCard.tsx: Renders question prompt, skill badge, and type-specific input:
   - MCQ (SPRT-04): 4 selectable option buttons.
   - Numerical (SPRT-05): Numeric input field with submit button.
   - Spatial (SPRT-06): 2x2 image grid of options (or fallback visual options if image URL unavailable).
</action>
<verify>
test -f client/src/components/QuestionCard.tsx && test -f client/src/components/TimerBar.tsx
</verify>
<acceptance_criteria>
- SkillBadge uses LinearGradient per skill type.
- TimerBar animates smooth countdown and turns red in final 25%.
- QuestionCard handles MCQ, numerical, and spatial question types.
</acceptance_criteria>
</task>

<task>
<id>07-02-02</id>
<type>code</type>
<files>client/app/sprint/[type].tsx, client/app/(tabs)/sprint.tsx</files>
<action>
1. Create client/app/sprint/[type].tsx: Active Sprint screen.
   - Fetches questions from GET /api/sprint?type={type} on mount.
   - Shows question count progress (e.g. "Question 3 of 10").
   - Renders TimerBar and QuestionCard.
   - Implements auto-advance (D-50): when an answer is selected/entered, flashes selection state for 300ms, records timeMs spent, and advances to next question.
   - On final question completion, submits all responses to POST /api/sprint/submit and navigates to /sprint/results with response data payload.
2. Update client/app/(tabs)/sprint.tsx: Sprint type launcher screen allowing user to choose Quick (5 questions), Standard (10 questions), or Deep (15 questions) (SPRT-02).
</action>
<verify>
test -f client/app/sprint/[type].tsx
</verify>
<acceptance_criteria>
- Active sprint screen fetches personalized questions and tracks per-question timing.
- Auto-advances smoothly on answer selection.
- Submits sprint responses on completion.
</acceptance_criteria>
</task>

<task>
<id>07-02-03</id>
<type>code</type>
<files>client/app/sprint/results.tsx</files>
<action>
Create client/app/sprint/results.tsx: Post-sprint summary and answer review screen (SPRT-07, SPRT-08).
- Displays total accuracy percentage, XP earned with speed bonus, and streak update.
- Shows per-skill rating deltas (e.g., Verbal +12 ELO, Quant +5 ELO).
- Provides scrollable per-question review list showing user answer vs correct answer, explanation text, and time spent per question.
- Includes a "Done" button that navigates back to Home/Tabs.
</action>
<verify>
test -f client/app/sprint/results.tsx
</verify>
<acceptance_criteria>
- Results screen displays accuracy, XP earned, streak updates, and rating deltas.
- Question review section shows detailed breakdown with explanations.
</acceptance_criteria>
</task>

</tasks>

<verification>
Ensure QuestionCard, TimerBar, active sprint screen [type].tsx, and results.tsx exist and compile without syntax errors.
</verification>

<success_criteria>
- Full sprint flow is complete from selection -> active quiz -> submission -> detailed results review.
</success_criteria>
