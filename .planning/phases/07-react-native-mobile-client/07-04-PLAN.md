---
phase: 7
plan: 4
type: execute
wave: 3
depends_on: [07-02, 07-03]
files_modified:
  - client/app/(tabs)/leaderboard.tsx
  - client/app/(tabs)/index.tsx
autonomous: true
requirements:
  - GAME-02
  - GAME-05
---

<objective>
Implement the Leaderboard screen displaying weekly XP rankings with user position highlighting, and polish the Home screen with streak count, XP summary, and quick-start sprint action cards.
</objective>

<tasks>

<task>
<id>07-04-01</id>
<type>code</type>
<files>client/app/(tabs)/leaderboard.tsx</files>
<action>
Implement client/app/(tabs)/leaderboard.tsx:
- Fetches GET /api/leaderboard on mount.
- Displays weekly league header (e.g. "Weekly League — Group 1").
- Displays scrollable leaderboard list showing rank (1, 2, 3 with gold/silver/bronze badges), user avatar/ID, and total weekly XP.
- Highlights current mock user's row distinctly.
</action>
<verify>
test -f client/app/(tabs)/leaderboard.tsx
</verify>
<acceptance_criteria>
- Leaderboard screen connects to GET /api/leaderboard and renders ranked weekly XP list.
</acceptance_criteria>
</task>

<task>
<id>07-04-02</id>
<type>code</type>
<files>client/app/(tabs)/index.tsx</files>
<action>
Polish client/app/(tabs)/index.tsx (Home screen):
- Fetches GET /api/users/me and GET /api/analytics/progress on mount.
- Header showing user greeting, daily streak counter with fire icon, and total XP badge.
- Main hero banner: "Daily Sprint" with quick launch button (Standard 10 questions).
- Sprint mode grid: Quick (5 q), Standard (10 q), Deep (15 q).
- Skill summary cards: Overview of 4 skill categories with progress scores and "Practice" action.
- Styled in dark theme with vibrant per-skill gradients.
</action>
<verify>
test -f client/app/(tabs)/index.tsx
</verify>
<acceptance_criteria>
- Home screen provides a polished Elevate-style dashboard with streak, XP, quick sprint start, and skill cards.
</acceptance_criteria>
</task>

</tasks>

<verification>
Ensure leaderboard.tsx and index.tsx exist, compile without errors, and integrate with backend API.
</verification>

<success_criteria>
- Leaderboard screen shows weekly rankings and user position.
- Home screen serves as the main hub with streak counter, sprint launcher, and skill cards.
</success_criteria>
