---
phase: 7
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - client/package.json
  - client/app/_layout.tsx
  - client/app/(tabs)/_layout.tsx
  - client/app/(tabs)/index.tsx
  - client/app/(tabs)/sprint.tsx
  - client/app/(tabs)/dashboard.tsx
  - client/app/(tabs)/leaderboard.tsx
  - client/src/theme.ts
  - client/src/api.ts
autonomous: true
requirements:
  - SPRT-01
---

<objective>
Scaffold the Expo React Native app in /client using Expo SDK 52 with Expo Router file-based navigation, set up dark mode design system + per-skill gradients, bottom tab bar (Home, Sprint, Dashboard, Leaderboard), and API client module.
</objective>

<tasks>

<task>
<id>07-01-01</id>
<type>code</type>
<files>client/package.json</files>
<action>
Scaffold a new Expo project in /client using npx create-expo-app@latest client --template default (or blank with expo-router). Ensure Expo SDK 52, expo-router, react-native-gifted-charts, expo-linear-gradient, react-native-svg, and axios are installed.
</action>
<verify>
test -f client/package.json
</verify>
<acceptance_criteria>
- client/package.json exists with expo, expo-router, react-native-gifted-charts, expo-linear-gradient dependencies.
</acceptance_criteria>
</task>

<task>
<id>07-01-02</id>
<type>code</type>
<files>client/src/theme.ts, client/src/api.ts</files>
<action>
1. Create client/src/theme.ts defining dark theme palette (background: #0B0F19, card: #151C2C, text: #F3F4F6, textMuted: #9CA3AF) and per-skill gradients (D-51, D-52):
   - verbal: ['#8B5CF6', '#3B82F6'] (purple -> blue)
   - quantitative: ['#14B8A6', '#10B981'] (teal -> green)
   - logical: ['#F97316', '#F59E0B'] (orange -> amber)
   - spatial: ['#EC4899', '#EF4444'] (pink -> red)
2. Create client/src/api.ts configured with Axios pointing to http://localhost:3000 (or IP / localhost fallback) for all endpoints: getSprint, submitSprint, getProgress, getHistory, getLeaderboard.
</action>
<verify>
test -f client/src/theme.ts && test -f client/src/api.ts
</verify>
<acceptance_criteria>
- client/src/theme.ts exports dark theme colors and skillGradients object.
- client/src/api.ts exports API helper functions for all endpoints.
</acceptance_criteria>
</task>

<task>
<id>07-01-03</id>
<type>code</type>
<files>client/app/_layout.tsx, client/app/(tabs)/_layout.tsx, client/app/(tabs)/index.tsx, client/app/(tabs)/sprint.tsx, client/app/(tabs)/dashboard.tsx, client/app/(tabs)/leaderboard.tsx</files>
<action>
1. Create client/app/_layout.tsx with Stack navigator and DarkTheme background context.
2. Create client/app/(tabs)/_layout.tsx with Tabs layout mapping 4 tabs: index (Home), sprint (Sprint), dashboard (Dashboard), leaderboard (Leaderboard). Configure dark theme tab bar styling and icons.
3. Create placeholder screens in client/app/(tabs)/ for index.tsx, sprint.tsx, dashboard.tsx, leaderboard.tsx using dark theme styles.
</action>
<verify>
test -f client/app/(tabs)/_layout.tsx
</verify>
<acceptance_criteria>
- Expo Router file-based tab bar structure created and correctly configured.
- Dark theme styling applied globally.
</acceptance_criteria>
</task>

</tasks>

<verification>
Check client directory structure and ensure package.json, theme.ts, api.ts, and app/(tabs)/_layout.tsx exist.
</verification>

<success_criteria>
- /client contains a valid Expo SDK 52 project with expo-router tabs layout.
- Theme system (dark mode + per-skill gradients) and API client are ready for screen components.
</success_criteria>
